[workspace]
resolver = "3"
members = [ "tacl", "billing", "documents", "framework", "tagui", "framework_derive_macros"]

[profile.dist]
inherits = "release"
opt-level = 3
lto = true
codegen-units = 1
strip = true
