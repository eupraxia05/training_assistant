[package]
name = "tagui"
version = "0.1.0"
edition = "2024"

[dependencies]
dolmen = {workspace = true}
reliquary = {workspace = true}
billing = { version = "0.1.0", path = "../billing" }
db_commands = { version = "0.1.0", path = "../db_commands" }
eframe = "0.33.0"
egui = "0.33.0"
gui = { version = "0.1.0", path = "../gui" }

[lints]
workspace = true
